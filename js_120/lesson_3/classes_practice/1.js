/*
1. What do we mean when we say that classes are first-class values

We mean that, since classes are functions which are objects, classes can 
be passed to functions as arguments, returned from functions, and assigned
to values in JavaScript.

*/