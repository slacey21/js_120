/*
7. The following code keeps looping forever. Why is that? 
Also modify it so that it stops after the first iteration.

for (let i = 0; ; i += 1) {
  console.log("and on");
}

The code runs forever because there is no termination condition in the loop to check against

*/

for (let i = 0; i < 1; i += 1) {
    console.log("and on");
}