4. What does the following code log to the console?

function scream(words) {
  words = words + '!!!!';
  return;
  console.log(words);
}

scream('Yipeee');

ANSWER:
The code does not log anything because the return statement
terminates the function before the code logs anything to the console.
